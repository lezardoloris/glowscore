import BodyCareHub from '../../src/components/BodyCareHub';

/** Persistent Body tab (PS-1.1 "entry from home + tab"): no back chevron, tab owns nav. */
export default function BodyTab() {
  return <BodyCareHub />;
}
