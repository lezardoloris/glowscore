// v1.2: This component is no longer used.
// Style cards are rendered inline in app/styles.tsx.
// All styles are free — no lock/premium gating needed.
// Kept as empty file to avoid import errors if referenced elsewhere.

export default function StylePreview() {
  return null;
}
